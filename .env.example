# =============================================================
# Mealing About — environment variables reference
# Copy the relevant section to each package's .env file.
# Never commit actual keys — this file is safe to commit.
# =============================================================

# -------------------------------------------------------------
# Google Places API  (packages/api, packages/pipeline)
# https://console.cloud.google.com → APIs & Services → Credentials
# Enable: Places API (New)
# -------------------------------------------------------------
GOOGLE_PLACES_API_KEY=

# -------------------------------------------------------------
# Supabase  (packages/api, packages/pipeline, apps/web)
# https://supabase.com → Project Settings → API
# -------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=           # safe for client-side / API reads
SUPABASE_SERVICE_ROLE_KEY=   # pipeline only — keep secret, bypasses RLS

# -------------------------------------------------------------
# AI providers  (packages/api, packages/pipeline)
# -------------------------------------------------------------

# Perplexity — primary text analysis provider
# https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=

# Google Gemini — secondary text + native PDF analysis (free tier)
# https://aistudio.google.com → Get API key
GEMINI_API_KEY=

# Anthropic Claude — fallback + scanned PDF vision
# https://console.anthropic.com → API Keys
ANTHROPIC_API_KEY=

# Menu analyzer provider for the API package (perplexity or anthropic)
MENU_ANALYZER_PROVIDER=perplexity

# -------------------------------------------------------------
# Mobile app  (apps/mobile)
# Use http://localhost:3000 for local dev, your Vercel URL for prod
# -------------------------------------------------------------
EXPO_PUBLIC_API_URL=http://localhost:3000
